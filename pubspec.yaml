name: hng_authentication
description: An Authentication package used in hng_internship_10. This package
  offer the client the ability to call the signup, signin, logout functions and
  also check currently logged in user.
version: 0.1.2
homepage: https://github.com/hngx-org/team-fl-package-

environment:
  sdk: '>=3.1.2 <4.0.0'
  flutter: ">=1.17.0"

dependencies:
  
  flutter:
    sdk: flutter
  google_fonts: ^6.1.0
  http: ^1.1.0
  mockito: ^5.4.2

dev_dependencies:
  flutter_lints: ^2.0.0
  flutter_test:
    sdk: flutter

# For information on the generic Dart part of this file, see the
# following page: https://dart.dev/tools/pub/pubspec
# The following section is specific to Flutter packages.
flutter: null

